#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

int _putchar(char c);
int is_digit(char *str);
int get_sum_of_args(int argc, char *argv[]);

#endif /* MAIN_H */
